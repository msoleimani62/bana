[package]
name = "bana-output-validator"
version.workspace = true
edition.workspace = true
publish = false
description = "Post-build APK health validation for bana — verifies the final artifact directly via its ZIP structure, deliberately never depending on aapt2."

[dependencies]
thiserror = "1.0"
zip = { version = "0.6", default-features = false, features = ["deflate"] }

[package]
name = "bana-build-driver"
description = "Executes the actual build (cargo-ndk, uniffi bindgen, gradlew) chosen automatically from a ProjectFingerprint, with readable error handling instead of raw stack traces."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
bana-types.workspace = true
bana-env-scanner = { path = "../bana-env-scanner" }
bana-project-analyzer = { path = "../bana-project-analyzer" }
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true
